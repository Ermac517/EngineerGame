[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "engineergame"
version = "0.1.0"
description = "Basic problem-solving utilities"
readme = "README.md"
requires-python = ">=3.8"
